import { SearchProductItem } from "src/app/autogenerated/catalog/models";
import { TagDto } from "../dtos/tag.dto";

export class SearchProductItemDto implements SearchProductItem{
  public id: string = "";
  public shortDescription: string = "";
  public shortName: string = "";
  public tags: TagDto[] = [];

  public constructor(dto?: SearchProductItem) {
    if (dto) {
      this.id = dto?.id;
      this.shortDescription = dto?.shortDescription;
      this.shortName = dto?.shortName;
      this.tags = dto?.tags;
    }
  }
}
